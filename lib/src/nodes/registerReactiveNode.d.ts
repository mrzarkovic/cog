import { Attribute, ReactiveNodesList } from "../types";
export declare function registerReactiveNode(elementId: number, reactiveNodes: ReactiveNodesList, element: HTMLElement, originalInvocation: string, attributes?: Attribute[]): void;
