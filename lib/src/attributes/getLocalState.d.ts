import { Attribute, ReactiveNode, State } from "../types";
export declare function getLocalState(parentId: number | null, attributes: Attribute[], globalState: State, reactiveNodes: ReactiveNode[], stateChanges?: string[]): State;
